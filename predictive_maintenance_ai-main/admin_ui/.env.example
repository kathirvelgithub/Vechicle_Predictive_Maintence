# Admin UI runtime settings
VITE_API_BASE_URL=http://localhost:8000/api
VITE_AUTH_BASE_URL=http://localhost:8080
# Keep false for UI-first development; set true when enabling auth gate.
VITE_ADMIN_AUTH_ENABLED=false
# Optional explicit websocket endpoint (leave empty for auto-derive)
# VITE_STREAM_WS_URL=ws://localhost:8000/api/stream/ws
